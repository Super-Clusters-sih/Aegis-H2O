[project]
name = "app"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "scikit-learn==1.9.1",
    "joblib",
    "numpy",
    "psycopg2-binary",
    "python-dotenv",
    "email-validator",
]
requires-python = "~=3.12.0"